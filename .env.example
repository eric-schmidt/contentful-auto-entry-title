# App Definition details.
CONTENTFUL_ORG_ID=
CONTENTFUL_APP_DEF_ID=
# App bundle uploads; Content model imports.
CONTENTFUL_ACCESS_TOKEN=
# Taxonomy Concept reads at Function runtime; Inlined at build time, so rotating it
# needs an `npm run build` + `npm run upload` to take effect.
CONTENTFUL_DELIVERY_KEY=
